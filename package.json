{
  "name": "agentic-wallet",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace @agentic-wallet/api run dev",
    "dev:web": "npm --workspace @agentic-wallet/web run dev",
    "dev:webapp": "npm --workspace @agentic-wallet/webapp run dev",
    "build:web": "npm --workspace @agentic-wallet/web run build",
    "build:webapp": "npm --workspace @agentic-wallet/webapp run build",
    "build:api": "npm --workspace @agentic-wallet/api run build",
    "build": "npm run build:web && npm run build:webapp && npm run build:api",
    "deploy:api": "npm --workspace @agentic-wallet/api run deploy",
    "deploy:worker": "npm run build:web && npm run build:webapp && npm run deploy:api"
  }
}
